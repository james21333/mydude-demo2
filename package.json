{
  "name": "mydude-demo2",
  "version": "0.2.0",
  "private": true,
  "description": "Demo2 My Dude fork from v1.4 for alternate create/turn-into experiments.",
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/validate.mjs",
    "deploy": "wrangler deploy",
    "test": "vite build && node scripts/validate.mjs",
    "dev": "vite --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
